type Recipe {
    id: ID!
    title: String
    thumbnail: String
    previewText: String
}

type Query {
    recipes(query: String!): [Recipe!]!
    recipe(query: String!, id: ID!): Recipe
}
